# /opt/homebrew/bin/cc is a symlink to ~/amux/amux (a wrapper that can't act as a
# linker). Force the real Apple clang so Rust build scripts and native deps link.
[target.aarch64-apple-darwin]
linker = "/usr/bin/cc"

[env]
CC = "/usr/bin/cc"
CXX = "/usr/bin/c++"

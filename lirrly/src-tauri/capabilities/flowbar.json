{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "flowbar",
  "description": "Floating bar overlay — least privilege: custom commands, events, and history writes only",
  "windows": ["flowbar"],
  "permissions": [
    "core:default",
    "store:default",
    "notification:default"
  ]
}
